# --- Core runtime ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
SQLAlchemy==2.0.36
python-dateutil==2.9.0.post0

# --- Quantitative modelling (brief 5.2) ---
numpy==2.2.1
pandas==2.2.3
scipy==1.15.0
scikit-learn==1.6.0
# xgboost is optional; enabled if non-linear demand patterns emerge
# xgboost==2.1.3

# --- Dashboard (brief 5.2: Streamlit MVP) ---
streamlit==1.41.1
pydeck==0.9.1          # map picker for parcel coordinates
plotly==5.24.1
requests==2.32.3
httpx==0.28.1         # FastAPI TestClient + dashboard API calls
openpyxl==3.1.5       # PropEquity Excel ingestion

# --- Ingestion: web scraping (brief 5.2) ---
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11
scrapy==2.16.0
# Playwright drives live JS-portal scraping (MagicBricks/99acres).
# After install, fetch the browser: playwright install chromium
playwright==1.49.1
# XGBoost for non-linear price modelling (brief 5.2). Needs libomp on macOS
# (brew install libomp); on Linux/AWS it works out of the box. The engine
# falls back to scikit-learn GradientBoosting automatically if unavailable.
xgboost==2.1.3

# --- Postgres driver (used when DATABASE_URL points at RDS) ---
psycopg[binary]==3.2.3

# --- Optional integrations (graceful import) ---
# anthropic==0.42.0          # only if GPL_LLM_PROVIDER=anthropic
# sendgrid==6.11.0           # only if email alerting enabled

# --- Testing ---
pytest==8.3.4
